nginx:
    build: ./nginx/
    ports:
        - 80:80
        - 443:443
    links:
        - php
    volumes_from:
        - data-www

php:
    build: ./php/
    expose:
        - 9000
    links:
        - mysql
    volumes_from:
        - data-www

data-www:
    image: busybox:latest
    volumes:
        - ~/Movies/html:/var/www/html
    command: "true"

mysql:
    image: mysql:latest
    volumes_from:
        - data-db
    environment:
        MYSQL_ROOT_PASSWORD: secret

data-db:
    image: busybox:latest
    volumes:
        - /var/lib/mysql
    command: "true"

phpmyadmin:
    image: phpmyadmin/phpmyadmin
    ports:
        - 8080:8080
    links:
        - mysql
    environment:
        PMA_HOST: mysql